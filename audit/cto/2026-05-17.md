# CTO audit log - 2026-05-17

## CLO-222 runtime-policy :: stability-l0a :: allow arch_prctl in seccomp

- Decision: final CTO disposition is `blocked`, not `in_progress`.
- Rationale: the `arch_prctl` seccomp amendment was already accepted as a narrow Stability-owned runtime-policy change. It removes the glibc TLS setup denial without weakening the hardened runtime profile.
- Follow-up path: `CLO-223` is the Stability-owned post-amendment L0a boot trace. It was reopened as `todo` because it had no upstream blocker.
- Dependency correction: `CLO-222` is now first-class blocked by `CLO-223`, so the parent resumes only after Stability posts the next boot-trace result.
- Rule posture: no CTO direct edit to `stack/`; no `seccomp=unconfined`; no `privileged:true`; no `:latest`; no paid RASP/DexGuard; no TEESimulator + TrickyStore composition.

## CLO-222 recovery cleanup

- Resolved Paperclip recovery action `dc1921bd-81e1-480c-8728-9f932b8f1623` as `blocked` after the first-class blocker path existed.
- Stability completed the post-`arch_prctl` trace on `CLO-223`: canonical L0a still exits `6`, and `SCMP_ACT_LOG` also exits `6`, so the next failure is not another seccomp allowlist miss.
- Live continuation moved to `CLO-230`, a container-unhealthy follow-up under Orchestrator/Stability ownership.

## CLO-210 productivity review for CLO-10

- Review trigger: no-comment streak on CLO-10 builder work.
- Current finding: the old no-comment streak is no longer the operative stop condition.
- Current live path: `CLO-10 -> CLO-211 -> CLO-222 -> CLO-223 -> CLO-230`; `CLO-230` is `in_progress`.
- Disposition: closed `CLO-210` as `done` because the source tree has an active continuation path and does not need a new CTO product-rule plan in this heartbeat.

## CLO-125 NEW-SOURCE-OK board gate

- Source: `yubunus/DeviceSpoofLab-{Magisk,Hooks}`.
- CTO finding: SHA pinning was fixed earlier, but the source org is not allowlisted, both modules are absent from `shared/spoofstack-modules.md`, license is `NOASSERTION` / no `LICENSE.txt`, and original inclusion had proposal-scope creep.
- Decision: not approved for builder integration pending human board decision.
- Action: created board approval `8fd607c9-001f-42e7-a0ff-796d62a7c228`.
- Issue disposition: moved `CLO-125` to `in_review` with the approval as the real waiting path.

## CLO-132 blocker cleanup

- Issue: `CLO-132` remained blocked on sandbox build/probe children, but the uncovered leaves were `CLO-171` and `CLO-172`.
- Finding: both leaves were Stability-owned `sandbox-build` issues marked `blocked` with no first-class blockers.
- Action: moved `CLO-171` and `CLO-172` back to `todo` for Stability with instructions to run the sandbox build or set a real blocker.
- Disposition: no CTO module approval yet; `CLO-132` remains blocked on child build/probe evidence, but the blockerless leaves now have a live path.

## CLO-222 final disposition after CLO-223 completion

- Stability child `CLO-223` completed the post-`arch_prctl` trace and confirmed the canonical seccomp profile includes `arch_prctl`.
- Finding: the remaining exit-6 is not an `arch_prctl` allowlist gap and was not surfaced by changing seccomp default action to logging.
- Decision: close `CLO-222` as `done`; the runtime-policy amendment and required post-amendment trace handoff are complete.
- Rejected path: no `privileged:true` or `cap_add: SYS_ADMIN` diagnostic is authorized from `CLO-222`. Per `refuse-privileged-compose`, that requires a separate board-opened `[PRIVILEGED-OK]` issue.
- Delegated follow-up: opened `CLO-232` for Stability to select or validate a ReDroid L0a baseline that boots under the current non-privileged hardening profile, or return a precise blocker.
