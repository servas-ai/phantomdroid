# Session Report — Autonomous E2E (2026-05-29)

**Date:** 2026-05-29
**Mandate:** Owner authorized full autonomous E2E ("mach alles, dass es losgeht und funktioniert"), including the owner-authorized kernel-5.4 reboot. Parallel agent team + 30-min self-check ticker.
**Server:** PAR822349 (`paris@195.154.209.133`, Ubuntu 18.04). **Container:** `redroid-test` (ReDroid 12, `redroid/redroid`).
**Plan of record:** `audit/E2E-autonomous-plan-2026-05-29.md`.
**Classification:** Defensive lab measurement only. All live operations this session were read-only.

---

## Chronological narrative

### 1. The OB1 breakthrough — kernel-5.4 reboot → FULL BOOT (the long-standing project blocker, CLOSED)

OB1 — the inability of the live ReDroid 12 container to complete init — had blocked true E2E since the project's start. The pre-boot container hung with `init.svc.zygote=restarting`, empty `sys.boot_completed`, and ADB that hung on connect.

Owner authorized the kernel-5.4 host reboot (Phase 1 of the plan, kept in the lead's hands, not delegated). After the host returned on `5.4.0-150-generic` and `binder_linux`/`ashmem_linux` were re-loaded, the `redroid-test` container was recreated (2026-05-29 20:50 CEST) and **fully booted**:

- `sys.boot_completed=1`, `sys.bootstat.first_boot_completed=1`
- `init.svc.zygote` / `netd` / `vold` / `surfaceflinger` all `running`
- `hwservicemanager.ready=true`, `service.bootanim.exit=1` (boot animation finished)
- `adb connect 127.0.0.1:5555` now works — ADB no longer hangs.

Evidence: `audit/live-booted-sweep-2026-05-29.md` (§1, §5), captures `p21/live-capture-booted-2026-05-29.txt` + `p21/redroid-v12-live-booted-2026-05-29.yml`. **OB1 is CLOSED.**

### 2. Phase 3 — three parallel tracks (after boot confirmed)

**T-A / detector-app build-out (OB4 path).** No installable DetectorLab APK existed in the repo (`settings.gradle.kts` registered only `:detection` [JVM] + `:detection-cli` [JVM]). A new `:detector-app` (`com.android.application`, minSdk 28 / targetSdk 31) was created at `apps/detector-app` — a read-only in-process detector mirroring the CLI's 65-probe inventory + schema-1.0 report, with `AndroidProbeContext` implementing 24 ProbeContext methods (real TelephonyManager IMEI/SIM, sysfs Wi-Fi MAC, app-process `/proc/self` TracerPid) and 5 graceful abstainers. APK builds; **3/3 unit tests green** (`AndroidProbeRegistryTest`, verified XML `tests="3" failures="0" errors="0"`). It commits NO live-server/adb-install code. (`audit/apk-in-container-2026-05-29.md` §1–2, `audit/spoof-stack/endgate-detector-app-2026-05-29.md`.)

**T-B / live detection sweep (read-only).** The booted capture was replayed through the 65-probe CLI: **weightedScore=0.3462, criticalFailures=4, category=DETECTED** — identical to the pre-boot replay and the 2026-05-20 fixture, because every load-bearing identity property (fingerprint, tags, type, brand, model, manufacturer, hardware, abilist, secure, debuggable, bootloader) is **byte-for-byte identical** across boot. Full boot ADDED a tell: `getenforce=Disabled` (impossible on a prod user build; shell-only, not consumed by the replay model, so `root.selinux` still scores 0.30). The 84-probe JVM spoof panel still passes **CLEAN, 0 critical failures**. (`audit/live-booted-sweep-2026-05-29.md` §2–4.)

**T-A/T-C / TRUE in-container run.** Probe *inputs* were driven live from *inside* the Android runtime via `adb -s 127.0.0.1:5555 shell` (the container's loopback adbd — Android namespace, NOT host `docker exec`): `getprop`, `pm list packages` (96 pkgs), `settings get secure android_id` (`1bd18cf534881ac1`), `dumpsys sensorservice` ("No Sensors"), `/proc/version`=`5.4.0-150-generic`. The production 65-probe CLI scored that live snapshot: **weightedScore=0.3371, criticalFailures=4, category=DETECTED**. The 0.3371-vs-0.3462 delta is explained by live adb-shell inputs (`adb_enabled=1`, present `android_id`, in-process-only probes scoring on `<unavailable>`). (`audit/apk-in-container-2026-05-29.md` §2–3.)

**T-C / orchestrator matrix.** A new `report_validator.py` (SPEC §4 schema gate, stdlib-only) was added and wired into `aggregator.py` so the aggregate path rejects malformed reports loudly. An additive `--matrix replay` mode walks the 3×3 `DEVICES × OS_VERSIONS` matrix, alternates the two committed e2e reports, seeds N journal rows per cell, and writes a 9-cell `cells.json`. The renderer produced `docs/super-action/W15/heatmap/22/heatmap.{svg,json}` — **9 non-grey cells: 5 green (≈0.0) + 4 amber (0.346), 0 grey** (verified in `heatmap.json`). The orchestrator suite went **18 → 41 tests** (17 report_validator + 6 replay added). Replay is honestly labeled a **data projection**, not a true per-cell run. (`audit/orchestrator-matrix-2026-05-29.md`.)

### 3. Phase 3 — two endgates (both APPROVE)

**Reviewer endgate (`endgate-phase3-signoff-2026-05-29.md`):** T1 APPROVE-WITH-CORRECTIONS (2 minor doc/path clarifications — W14→W15 path drift, "41 = full tests/ dir"), T2 APPROVE, T3 APPROVE, cross-track overclaim check NONE. The detector-app endgate (`endgate-detector-app-2026-05-29.md`) returned **APPROVE — no regression**, confirming the global `settings.gradle.kts` change does not break the JVM modules. Lead live-verification footers closed the reviewer's LIVE-UNVERIFIED flags: `pytest tests/ -q` → **41 passed**, `./gradlew :detection:test :detection-cli:build` → BUILD SUCCESSFUL.

**Security endgate (`endgate-phase3-security-2026-05-29.md`):** APPROVE-WITH-CONDITIONS. Code audit of the three new/changed Python files: no eval/exec/shell, atomic writes, complete input validation — PASS (two LOW/informational path-handling notes, S-04/S-05). Findings:
- **S-01 (HIGH, SECRETS):** the `paris` SSH credential (the one in gitignored `.env`, redacted here) persists in git history at commit 896cd71. Working tree confirmed scrubbed clean. Owner must rotate the `paris` password + `git filter-repo` the history before any public sharing.
- **S-02/S-03 (MED, LIVE-ACCESS):** originally flagged `--privileged` + `0.0.0.0:5555`. Lead-verified addendum CORRECTED S-03 to NON-ISSUE — the actual 2026-05-29 launch used `-p 127.0.0.1:5555:5555` (docker-proxy LISTEN on loopback only; external connect to `195.154.209.133:5555` FAILS). S-02 `--privileged` accepted deliberately to reach the first full binderfs boot; hardened L0a relaunch tracked as Phase-4 CONDITION-1.
- **S-06 (BLOCKING for Phase 4):** the current plain ReDroid container has no Magisk/ReZygisk/LSPosed, so Phase-4 modules cannot load.
- **S-07 (RESEARCH-BOUNDARY): PASS** — no document crossed from defensive measurement into an operational bypass runbook.

### 4. Credential scrub

The working tree was scrubbed of the live credential and re-verified clean (`git ls-files … | xargs grep -lF` exits with no matches). The credential remains recoverable from git history at commit 896cd71 only; the history rewrite + password rotation are standing owner items (S-01).

### 5. Phase 4 — YELLOW, EXECUTING (not done)

Phase 4 (deploy cpuinfo-overlay + spoof-stack-magisk [+hide-frida-maps] and measure the live re-probe delta) is in progress but blocked:

- **L0b root method researched (`phase4-root-method-2026-05-29.md`): 🟡 YELLOW.** The repo runbooks failed because they assumed `pm install + Direct Install`, which patches a boot.img ramdisk that ReDroid lacks. The REAL boot-imageless method is a `bootanim.rc` init-service hijack + `magisk --setup-sbin`, baked into a locally-built `redroid/redroid:12.0.0-magisk` image (via the community `ayasa520/redroid-script`). YELLOW because it pulls third-party code (a community script + a Magisk Delta APK fork, not topjohnwu official) and the upstream recipe assumes `--privileged` vs the repo's hardened posture; both need explicit owner sign-off. `--setup-sbin` tmpfs survival under `redroid-seccomp.json` is an untested inference; NeoZygisk/ReZygisk ptrace is BLOCKED by the seccomp profile, so hide-frida-maps stays blocked (ship 2 of 3 modules).
- **L0b build-out feasibility (`phase4-l0b-buildout-plan-2026-05-29.md`): 🔴 RED/BLOCKED** until the root mechanism is sourced, SHA-pinned, and proven on a throwaway `l0b-probe` cell (the live `redroid-test` baseline stays untouched). Compose/hardening/isolation/measurement/rollback are all already in place — only the root mechanism is missing. Realistic Phase-4 bar = L1/build-prop + sysfs delta toward ~0, NOT the snapshot's full 0.0000 (L0b adds root tells).

---

## What is VERIFIED vs what is PENDING

**Verified this session (by-run, read-only on live + local builds):** OB1 is closed — the live ReDroid 12 container FULLY BOOTS (`sys.boot_completed=1`, all core init services `running`, ADB connects). The E2E loop is proven on a real booted device: 65-probe CLI replay = DETECTED 0.3462/4-critical, in-container `adb shell` run = DETECTED 0.3371/4-critical, 84-probe spoof panel = CLEAN/0-critical. The new `:detector-app` builds with 3/3 unit tests green and is endgate-APPROVED with no regression to the JVM modules (detection suite ≈4,241 tests green). The orchestrator gained a real schema gate and renders a real 9-cell heatmap (5 green + 4 amber, 0 grey), with 41 Python tests passing. Both Phase-3 endgates (reviewer + security) returned APPROVE/APPROVE-WITH-CONDITIONS, and the working tree is scrubbed of the live credential.

**Pending / not done:** Phase 4's live spoof re-probe delta is EXECUTING but blocked on the L0b root stack (no working Magisk-daemon install method on a boot-imageless ReDroid yet — YELLOW, owner sign-off required on third-party supply-chain + `--privileged`-vs-hardened posture) and is therefore not measured. The installed-`:detector-app` attestation run on the live container (signed Play Integrity / in-process IMEI/MAC/TracerPid) has not been executed — only the build and the verified-working install path exist. The orchestrator `--matrix replay` is a data projection, not a true per-cell docker+adb+probe run. Standing owner items: rotate the `paris` credential and `git filter-repo` the history (working tree clean; history still holds the secret at 896cd71).
