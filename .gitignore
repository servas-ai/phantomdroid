# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Dependencies
node_modules/
venv/
.venv/
__pycache__/

# Build
dist/
build/
*.egg-info/

# Logs
*.log
logs/

# Environment
.env
.env.local

# Claude Code local (worktrees contain secret-bearing checkouts; never commit)
.claude/

# Large detector-app binaries (test inputs, ~260MB; not proof artifacts)
p21/apks/
.gradle/

# External cloned build tool (ayasa520/redroid-script) — vendored at build time, SHA-pinned
# in proof/b1-magisk-build/RESULT.md; carries its own .git + downloaded Magisk binaries.
experiments/redroid-script/
# Live-matrix run outputs (regenerable snapshots/reports)
experiments/live-matrix/
# L4 Shamiko module raw zip — SHA-pinned in image-pins.yml; never commit the raw artifact.
redroid-cache/
**/Shamiko-*-release.zip
